@model IEnumerable<Chat>

@{

}

<div class="side-menu">
    @foreach (var chat in Model)
    {
        <a class="room-button" href="#">
            @chat.Name
        </a>
    }

    <button type="button" class="room-button" id="create-room-btn">
        <strong>+</strong>
    </button>

</div>
